# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files
.env
backend/.env

# Databases — contain real user data (PII + password hashes). NEVER commit.
*.db
*.sqlite
*.sqlite3

# Rust build artifacts
/backend/target/

# Archives & backups
*.zip
*.tar.gz
*.bak

# Runtime files
*.pid

# Stray screenshots directory (typo'd name, ad-hoc captures)
/screeshot/

# Separate built app (the "MuscleRace" game) served at /musclerace/ — not part
# of this site's source. Lives on disk / served by nginx, but kept out of the repo.
/public/musclerace/
